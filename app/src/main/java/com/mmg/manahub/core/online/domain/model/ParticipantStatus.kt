package com.mmg.manahub.core.online.domain.model

/** [UNKNOWN] is a non-terminal placeholder for a server value this client build does not know. */
enum class ParticipantStatus { JOINED, OFFLINE, LEFT, UNKNOWN }
